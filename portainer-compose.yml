version: '3.8'
services:

  # Building and testing environment
  app:
    image: ghcr.io/alymasani/toolbox:ensf400_w25_alymasani
    stdin_open: true
    tty: true
    command: ["/bin/zsh"]
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    network_mode: "host"
